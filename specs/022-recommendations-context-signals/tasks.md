# Tasks: Recommendations Persistence, User Context, and Signal Verification

**Input**: Design documents from `/specs/022-recommendations-context-signals/`
**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/api.md

**Tests**: Not explicitly requested in the spec. Test tasks omitted.

**Organization**: Tasks are grouped by user story to enable independent implementation and testing of each story.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)
- Include exact file paths in descriptions

---

## Phase 1: Foundational (Blocking Prerequisites)

**Purpose**: Rename `consult_logs` → `recommendations` across the entire stack. All three user stories depend on this rename.

**CRITICAL**: No user story work can begin until this phase is complete.

- [x] T001 Create Alembic migration to rename `consult_logs` table to `recommendations` and rename index `ix_consult_logs_user_id` → `ix_recommendations_user_id` in alembic/versions/. Use `ALTER TABLE RENAME` (metadata-only, instant). Run `poetry run alembic revision --autogenerate -m "rename_consult_logs_to_recommendations"` then replace autogenerated content with manual `op.rename_table` + `op.execute` for index rename.
- [x] T002 [P] Rename ORM model `ConsultLog` → `Recommendation` in src/totoro_ai/db/models.py. Change `__tablename__` from `"consult_logs"` to `"recommendations"`. Update docstring. Keep all columns identical (id, user_id, query, response, created_at). Update `__init__.py` exports if any.
- [x] T003 [P] Rename repository file src/totoro_ai/db/repositories/consult_log_repository.py → src/totoro_ai/db/repositories/recommendation_repository.py. Rename classes: `ConsultLogRepository` → `RecommendationRepository` (Protocol), `SQLAlchemyConsultLogRepository` → `SQLAlchemyRecommendationRepository`, `NullConsultLogRepository` → `NullRecommendationRepository`. Update imports of `ConsultLog` → `Recommendation`. Keep `save()` method identical.
- [x] T004 Update src/totoro_ai/api/deps.py: rename `get_consult_log_repo` → `get_recommendation_repo`, update imports to use `RecommendationRepository`/`SQLAlchemyRecommendationRepository` from new path, update `get_consult_service()` to pass `recommendation_repo` instead of `consult_log_repo`.
- [x] T005 Update src/totoro_ai/core/consult/service.py: rename constructor param `consult_log_repo` → `recommendation_repo`, rename `self._consult_log_repo` → `self._recommendation_repo`, rename method `_persist_consult_log` → `_persist_recommendation`, update internal `ConsultLog(...)` → `Recommendation(...)` construction. Update all imports.

**Checkpoint**: `poetry run alembic upgrade head` succeeds. `poetry run ruff check src/` and `poetry run mypy src/` pass. Existing consult flow works identically with renamed table.

---

## Phase 2: User Story 1 — Consult Persists Recommendation with Trackable ID (Priority: P1)

**Goal**: Every consult response includes a `recommendation_id` from the database.

**Independent Test**: Send `POST /v1/chat` with a consult-intent message → response `data.recommendation_id` is a UUID string → row exists in `recommendations` table.

- [x] T006 [US1] Add `recommendation_id: str | None = None` field to `ConsultResponse` in src/totoro_ai/api/schemas/consult.py. Add Field with description.
- [x] T007 [US1] Modify `_persist_recommendation()` in src/totoro_ai/core/consult/service.py to return `str | None` instead of `None`. On success: create `Recommendation` instance, call `self._recommendation_repo.save(rec)`, return `str(rec.id)`. On exception: log warning, return `None`. Keep the existing try/except pattern.
- [x] T008 [US1] Update `consult()` method in src/totoro_ai/core/consult/service.py: call `recommendation_id = await self._persist_recommendation(...)` before building `ConsultResponse`, pass `recommendation_id=recommendation_id` to the `ConsultResponse` constructor.

**Checkpoint**: Consult response includes `recommendation_id`. DB write failure returns `recommendation_id: null` without blocking response.

---

## Phase 3: User Story 2 — Product App Fetches User Context (Priority: P2)

**Goal**: `GET /v1/user/context?user_id=<id>` returns saved count and taste chips.

**Independent Test**: `curl http://localhost:8000/v1/user/context?user_id=<id>` returns JSON with `saved_places_count` and `chips`. New user returns zeros/empty.

- [x] T009 [P] [US2] Create Pydantic schemas in src/totoro_ai/api/schemas/user_context.py: `ChipResponse` (label: str, source_field: str, source_value: str, signal_count: int) and `UserContextResponse` (saved_places_count: int, chips: list[ChipResponse]). No `user_id` in response body. Import `Chip` from `src/totoro_ai/core/taste/schemas.py` to verify field alignment.
- [x] T010 [US2] Create route handler in src/totoro_ai/api/routes/user_context.py: `GET /user/context` with `user_id: str = Query(...)`. Inject `TasteModelService` via `Depends`. Call `get_taste_profile(user_id)`. If `None`: return `UserContextResponse(saved_places_count=0, chips=[])`. If present: extract `signal_counts["totals"]["saves"]` for `saved_places_count`, pass `chips` through as-is. Facade pattern — one service call only.
- [x] T011 [US2] Add `TasteModelService` dependency provider in src/totoro_ai/api/deps.py if not already exposed as a standalone dependency (currently constructed inline in `get_event_dispatcher`). Create `get_taste_service()` that returns `TasteModelService(session_factory=_get_session_factory())`.
- [x] T012 [US2] Register `user_context_router` in src/totoro_ai/api/main.py: import router from `routes.user_context`, add `router.include_router(user_context_router, prefix="")`.

**Checkpoint**: `GET /v1/user/context?user_id=<id>` returns valid JSON. Missing `user_id` returns 422. New user returns `saved_places_count: 0, chips: []`.

---

## Phase 4: User Story 3 — Signal Endpoint Replaces Feedback Endpoint (Priority: P3)

**Goal**: `POST /v1/signal` validates `recommendation_id` exists, dispatches event, returns 202. `POST /v1/feedback` is deleted.

**Independent Test**: POST with valid `recommendation_id` → 202. POST with bogus ID → 404. Unknown `signal_type` → 422.

- [x] T013 [P] [US3] Create Pydantic schemas in src/totoro_ai/api/schemas/signal.py: `SignalRequest` with `signal_type: Literal["recommendation_accepted", "recommendation_rejected"]`, `user_id: str`, `recommendation_id: str`, `place_id: str` (all required, all with Field descriptions). `SignalResponse` with `status: str = "accepted"`.
- [x] T014 [P] [US3] Add `exists()` method to `RecommendationRepository` protocol and implementations in src/totoro_ai/db/repositories/recommendation_repository.py. Protocol: `async def exists(self, recommendation_id: str) -> bool`. SQLAlchemy impl: `SELECT 1 FROM recommendations WHERE id = :id LIMIT 1` using `select(literal(1)).where(Recommendation.id == recommendation_id)`, return `result.scalar_one_or_none() is not None`. Null impl: return `True`.
- [x] T015 [US3] Add `get_recommendation_repo` as a dependency in src/totoro_ai/api/deps.py that can be injected into the signal route (if not already exposed from T004 — verify it's importable by route handlers).
- [x] T016 [US3] Create route handler in src/totoro_ai/api/routes/signal.py: `POST /signal` with `status_code=202`. Inject `EventDispatcher` via `Depends(get_event_dispatcher)` and `RecommendationRepository` via `Depends(get_recommendation_repo)`. Validate `recommendation_id` exists via `repo.exists()` — raise `HTTPException(404, detail="Recommendation not found")` if not. Create `RecommendationAccepted` or `RecommendationRejected` event based on `signal_type`. Dispatch via `event_dispatcher.dispatch(event)`. Return `SignalResponse()`. Pydantic's Literal type handles 422 for unknown signal_type automatically.
- [x] T017 [US3] Delete src/totoro_ai/api/routes/feedback.py and src/totoro_ai/api/schemas/feedback.py.
- [x] T018 [US3] Update src/totoro_ai/api/main.py: remove `feedback_router` import and `router.include_router(feedback_router, ...)`. Add `signal_router` import from `routes.signal` and `router.include_router(signal_router, prefix="")`.

**Checkpoint**: `POST /v1/signal` with valid ID → 202. Bogus ID → 404. Bad signal_type → 422. `POST /v1/feedback` → 404 (deleted). Langfuse shows trace for handler execution.

---

## Phase 5: Polish & Cross-Cutting Concerns

**Purpose**: Documentation updates, ADR, constitution, api-contract, verify all checks pass.

- [x] T019 [P] Add ADR-060 to docs/decisions.md: supersedes ADR-053 — `consult_logs` renamed to `recommendations`, two new endpoints added (`GET /v1/user/context`, `POST /v1/signal`), `POST /v1/feedback` deleted. This repo now writes to `recommendations` instead of `consult_logs`.
- [x] T020 [P] Update docs/api-contract.md: add `GET /v1/user/context` and `POST /v1/signal` sections per contracts/api.md. Remove any reference to `POST /v1/feedback`. Update consult response example to include `recommendation_id`. Update "Database tables FastAPI writes to" list: replace `consult_logs` with `recommendations`.
- [x] T021 [P] Update .claude/constitution.md: Section VI — replace `consult_logs` with `recommendations` in the owned tables list. Section VIII — add `GET /v1/user/context` and `POST /v1/signal`, remove `POST /v1/feedback` reference if present.
- [x] T022 Run full verification: `poetry run ruff check src/ tests/` and `poetry run ruff format src/ tests/` and `poetry run mypy src/` and `poetry run alembic upgrade head`. Fix any issues.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (Foundational)**: No dependencies — start immediately. BLOCKS all user stories.
- **Phase 2 (US1)**: Depends on Phase 1 completion.
- **Phase 3 (US2)**: Depends on Phase 1 completion. Independent of US1.
- **Phase 4 (US3)**: Depends on Phase 1 completion (needs `RecommendationRepository`). Depends on T014 (`exists()` method). Independent of US2.
- **Phase 5 (Polish)**: Depends on all user stories being complete.

### User Story Dependencies

- **US1 (P1)**: Depends on foundational rename only. No cross-story dependencies.
- **US2 (P2)**: Depends on foundational rename only. Fully independent of US1 and US3.
- **US3 (P3)**: Depends on foundational rename (for `RecommendationRepository.exists()`). Logically tests against US1 data (recommendation_id from consult) but is independently implementable.

### Within Each Phase

- Tasks marked [P] within a phase can run in parallel.
- Sequential tasks within a phase depend on prior tasks in that phase.

### Parallel Opportunities

- T002 and T003 can run in parallel (different files).
- T009 and T013 and T014 can run in parallel (different files, no dependencies on each other — only on Phase 1).
- T019, T020, and T021 can run in parallel (different doc files).
- US2 and US3 can be worked on in parallel after Phase 1 completes (if desired).

---

## Parallel Example: Phase 1

```
; These can run in parallel (different files):
T002: Rename ORM model in src/totoro_ai/db/models.py
T003: Rename repository file and classes

; Then sequential (depends on T002+T003):
T004: Update deps.py imports
T005: Update ConsultService references
```

## Parallel Example: US2 + US3

```
; After Phase 1 completes, these can start in parallel:
T009: [US2] Create user_context schemas
T013: [US3] Create signal schemas
T014: [US3] Add exists() to RecommendationRepository
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Foundational rename (T001–T005)
2. Complete Phase 2: US1 — recommendation_id in consult (T006–T008)
3. **STOP and VALIDATE**: Consult returns recommendation_id, row in DB
4. Deploy if ready — US2 and US3 can follow independently

### Incremental Delivery

1. Phase 1 → Foundation ready (table renamed, ORM/repo updated)
2. Add US1 (T006–T008) → Consult returns recommendation_id → Verify
3. Add US2 (T009–T012) → User context endpoint works → Verify
4. Add US3 (T013–T018) → Signal endpoint works, feedback deleted → Verify
5. Phase 5 (T019–T022) → Docs updated, all checks pass → Complete

---

## Notes

- Column name stays `response` (not `response_json`) — research.md Decision 1
- `TasteProfile.signal_counts` is a dict at the Pydantic boundary — access `["totals"]["saves"]` for saved count
- Existing `EventDispatcher` already has `recommendation_accepted` and `recommendation_rejected` handlers registered — no new handler wiring needed for US3
- The `RecommendationAccepted` and `RecommendationRejected` event classes already exist in the events module — reuse them
- `Literal` type on `signal_type` gives automatic 422 from Pydantic for unknown values — no manual validation needed
