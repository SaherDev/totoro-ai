# Model Selection — Committed to git, tracked for evaluations
# Do not put secrets in this file. API keys go in .local.yaml

models:
  intent_parser:
    provider: openai
    name: gpt-4o-mini

  orchestrator:
    provider: anthropic
    name: claude-sonnet-4

  embedder:
    provider: voyage
    name: voyage-3.5-lite
